[package]
name = "hypervisor"
version = "0.1.0"
edition = "2024"

[dependencies]
spin = "0.10.0"

allocator.workspace = true
fdt.workspace = true
string_utils.workspace = true
arch = { version = "0.1.0", path = "../arch" }
log = { version = "0.4.28", features = ["max_level_debug"]}
mmio_core = { version = "0.1.0", path = "../support/mmio_core" }
ns16550 = { version = "0.1.0", path = "../guest/devices/serial/ns16550" }

[features]
nested_support = []
