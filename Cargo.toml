[package]
name = "hypervisor"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 0

[dependencies]
byteorder = { version = "1", default-features = false }
arrayvec = { version = "0.7.6", default-features = false }
